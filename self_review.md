# 가장 잘했다고 생각하는 부분
이번 기말 프로젝트를 진행하면서 가장 신경 써서 프로젝트를 구현했던 점은 객체지향 설계 원칙을 지키면서 코드를 구성하는 것이었습니다. 처음에는 막연하게 기능을 구현하는 데만 집중했었지만, 부모 클래스인 Task를 만들고 이를 상속받으며 TodoItem과 Checklist를 구현하면서 코드의 중복을 줄일 수 있었습니다. 공통된 로직은 부모 클래스에서 처리하고 각자의 클래스에서는 필요한 기능만 구현하다보니, 전체적인 구조가 훨씬 깔끔해졌고 수정이 필요할 때도 어디를 고쳐야 할지 명확해져서 효율적으로 작업할 수 있었습니다.
# 아쉬운 부분
프로젝트 초반에 테스트 코드를 작성하는 과정에서 시행착오를 많이 겪었던거 같습니다. 패키지 경로 설정이 익숙하지 않았기에 pytest 실행 시 오류가 자주 발생하게 되었고, 이를 해결하는 데 생각보다 많은 시간이 소요되었습니다. 미리 패키지 구조에 대해 이해를하고 시작했더라면 좀 더 원활하게 프로젝트를 진행할 수 있었을 것이라고 생각하였고, 중간에 설계 부분들을 수정하느라 속도가 더뎌진 점이 조금 아쉬웠던거 같습니다.
# 다음에 개선하고 싶은 점
이번 프로젝트에서 가장 도전하고 싶었던 기능은 '마감 시간이 지나면 자동으로 상태가 변하는 기능'이었습니다. 처음에는 단순하게 datetime 비교를 통해 구현하려 했으나, 실시간으로 변화를 감지하거나 사용자가 프로그램 실행 중에 즉각적으로 완료 여부를 갱신하는 로직을 완벽하게 구현하는 데에는 기술적인 한계를 느꼈습니다. 덕분에 단순히 시간 비교 로직을 넣는 것보다, 프로그램의 전체적인 상태 관리와 비동기적인 처리가 왜 필요한지 고민해 볼 수 있는 계기가 되었습니다. 다음에는 이 부분을 제대로 공부하여 사용자가 별도의 조작을 하지 않아도 시간의 흐름에 따라 프로그램이 능동적으로 반응하도록 꼭 개선해 보고 싶습니다.

활용도구 : Gemini(AI챗봇)
활용 내용:
1.프로젝트의 전체적인 디렉토리 구조 및 패키지화 방식에 대한 표준 지침 확인
2.pytest 실행 시 발생하는 'Collection Error'의 원인을 찾고, 절대 경로를 이용한 import 방식 해결법 학습
3.docstring과 doctest 형식을 프로젝트에 적용하는 방법 학습
4.기존에 작성한 코드를 객체지향 설계 원칙(상속 및 비공개 메서드 활용)에 맞춰 리팩토링하는 과정에서 구조적인 조언을 얻음

# 코드 알아보기
#while True: 포로그램을 종료하기 전까지 계속 메뉴를 보여 주기 위해 무한루프를 돌림. '메인메뉴'와 '상세 메뉴' 분리 합니다.
#사용자 입력에서 불필요한 공백을 제거(strip)하고, 대문자를 소문자로 바꿔(lower) 입력값 오류를 줄였습니다.
 #enumerate함수를 통해 리스트의 요소뿐만 아니라 번호까지 함께 가져왔습니다.여기서 1을 넣은 이유는 리스트는 0부터 시작하지만,보여줄 떄는 1번부터 시작하도록 하기 위함입니다.
#sorted(schedules, key=lambda x: (x.date, x.time)): schedules 안의 객체들을 date(날짜) 먼저, 그다음 time(시간) 순서대로 정렬하라는 뜻입니다.
lambda x: not x.is_completed: 체크리스트에서 완료된 항목을 상단으로 올리기 위한 정렬 로직입니다.
except ValueError: 만약 숫자 변환 등에서 문제가 생기면, 프로그램 죽이지 말고 이 오류 메시지 출력하고 넘어가라는 뜻 입니다.